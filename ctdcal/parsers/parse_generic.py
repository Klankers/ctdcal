"""
Parsing module for generic discrete data, referenced to a cast and bottle
number (if available).

Note: A user configuration or path is required for this.
"""


def parse_xlsx_inventory(filename):
    #   Load the user config to pull the respective sheets and columns
    #   Station, Cast, Bottle, and any known REFT, SALT, OXY, or NUT values
    #   that will need to be appended.
    pass


def parse_txt_log():
    #   For groups that submit an ASCII, delimited file for a specific cast
    #   (denoted by filename)
    pass
